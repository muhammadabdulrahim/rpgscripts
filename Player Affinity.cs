public enum PlayerAffinity
{
	//	Specifying 0 is unnecessary, but added for extra clarity
	NONE = 0,
	LIGHT,
	DARK,

	//	This is a "trick" to get the length of the enum with PlayerAffinity.Length
	Length
}
